todo

- add pixel borders in texture data to remove seams in tiles and UI at non-native zoom
  - also add mipmaps to remove shimmering

- window management - borderless, predefined coords, size?

- add proper MSAA to minimap?


plugin ideas

- player list

- remove flash/screenshake: worldView.shakeMagnitude, etc -> replace with an indicator

- own shell marker

- extra info overlay (enemy hp/armor, hitboxes, info about interacts, ...)

- chat (popout, keep log, tabs, extra commands, etc)

- hide noise: friendly bullets, useless animations (ANIM_DESTROY_* if no dmg/hitbox)

- account switcher

